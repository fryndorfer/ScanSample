<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:vm="clr-namespace:ScanSample.ViewModels"
			 x:Class="ScanSample.Views.MainPage"
			 x:DataType="vm:MainViewModel"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:scanner="clr-namespace:BarcodeScanning;assembly=BarcodeScanning.Native.Maui"
             Unloaded="ContentPage_Unloaded">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Appearing" Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>
    <Grid RowDefinitions="*,100" HorizontalOptions="Center">
        <scanner:CameraView x:Name="myCameraView" Grid.Row="0" HeightRequest="500" WidthRequest="300"
                            CameraEnabled="{Binding IsCameraEnabled}" OnDetectionFinishedCommand="{Binding DetectionFinishedCommand}"
                            PoolingInterval="2000" PauseScanning="{Binding IsPauseScanning}" />
    </Grid>
</ContentPage>
